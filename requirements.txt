# Qwen3-TTS Dependencies
# Python 3.12+ required

# Core TTS
qwen-tts>=0.0.5

# PyTorch (will be installed with qwen-tts but specified for clarity)
torch>=2.0.0
torchaudio>=2.0.0

# Audio processing
soundfile>=0.12.0

# Web interface
gradio>=4.0.0

# For Ollama integration
requests>=2.28.0

# Optionally for acceleration (if needed)
# accelerate>=0.20.0
